name: retrain

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retrain:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Retrain placeholder
        run: echo "Retrain workflow scaffolded"
